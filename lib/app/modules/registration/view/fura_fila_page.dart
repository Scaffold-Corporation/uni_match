import 'package:flutter/material.dart';

class FuraFilaPage extends StatelessWidget {
  const FuraFilaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
